{
  let a = 10,
    b = 2,
    c = 2;

  function minusdelit() {
    firstMove = a - b;
    secondMove = firstMove / c;
    console.log("Результат вычисления:", secondMove);
  }
  minusdelit(a, b, c);
}

// Не забыть спросить на счёт task_13